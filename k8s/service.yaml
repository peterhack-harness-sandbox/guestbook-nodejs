---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-nodejs
  labels:
    app: guestbook-nodejs
spec:
  type: LoadBalancer
  #type: NodePort
  ports:
    # the port that this service should serve on
  - port: 3000
  - TargetPort: 3000
